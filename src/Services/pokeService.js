/*
import api from './api';

const findAllPokemons = () =>
    api.get(`/pokemon/`)
    .then ((response) => response)
    .catch((err) => err)

    const findDetails = (i) =>
    api.get(`/pokemon/${i}`)
    .then ((response) => response)
    .catch((err) => err) 
    

export {
    findAllPokemons,
    findDetails,
    
    
}
*/
